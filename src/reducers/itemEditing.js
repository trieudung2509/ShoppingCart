
var initState = {};

const itemEditing = (state = initState,action) => {
  return [];
  // switch (action) {
  //   case action:
  //
  //     break;
  //   default:
  //     return state;
  // }
}

export default itemEditing;
